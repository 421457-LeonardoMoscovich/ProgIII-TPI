spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pokemontcg
    username: pokemontcg
    password: pokemontcg
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true

logging:
  level:
    com.utn.pokemontcg: DEBUG
    org.hibernate.SQL: DEBUG
